import React from 'react';
import { Box, Typography } from '@mui/material';

const LayersContent: React.FC = () => {
  return (
    <Box sx={{ padding: 2 }}>
      <Typography variant="h6">Layers Content</Typography>
      <Typography variant="body1">This is where you can put the content related to Layers.</Typography>
      {/* Add your specific functionality or content here */}
    </Box>
  );
};

export default LayersContent;
